name: csv_data_source
description: App package to provide data from csv table
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  csv: ^5.0.1
  entity:
    path: ../entity
  repository:
    path: ../repository

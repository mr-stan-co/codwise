name: repository
description: Dart package which holds all repositories
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  intl: ^0.18.1
  csv: ^5.0.1
  entity:
    path: ../entity
